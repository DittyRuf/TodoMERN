import styled from './EditTodo.module.scss';

const EditTodo = () => {
	return (
		<div className={styled.edit}>
			<button className={styled.edit__button}>Hi</button>
		</div>
	);
};

export default EditTodo;
